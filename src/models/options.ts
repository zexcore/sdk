export type ZexcoreOptions = {
  /**
   * Your zexcore API key. Required to access the Zexcore API endpoints.
   */
  apiKey: string;

  /**
   * Your project id.
   */
  projectId: string;

  /**
   * The endpoint that your project uses.
   */
  endpoint: string;
};
